class AuthAnimationConstants {
  static const Duration headerAnimationDuration = Duration(milliseconds: 300);
  static const Duration formAnimationDuration = Duration(milliseconds: 400);
  static const Duration buttonAnimationDuration = Duration(milliseconds: 500);
  static const Duration termsAnimationDuration = Duration(milliseconds: 600);

  static const Duration headerDelay = Duration.zero;
  static const Duration subtitleDelay = Duration(milliseconds: 100);
  static const Duration formDelay = Duration(milliseconds: 200);
  static const Duration buttonDelay = Duration(milliseconds: 400);
  static const Duration termsDelay = Duration(milliseconds: 300);

  static const double verticalSpaceSmall = 5;
  static const double verticalSpaceMedium = 10;
  static const double verticalSpaceLarge = 22;
  static const double verticalSpaceXLarge = 38;
  static const double horizontalPadding = 20;
}
